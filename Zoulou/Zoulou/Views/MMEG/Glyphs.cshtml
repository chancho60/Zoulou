@model Zoulou.ViewModels.MMEG.GlyphViewModel
@{
    ViewBag.Title = "MMEG - Glyphs";
}
<div class="row">
    <div class="col-md-12">
        <table class="boxed">
            <thead>
                <tr>
                    <th class="boxed-gray">Rarity</th>
                    @foreach(var Rarity in Model.AvailableRarities) {
                        <th class="boxed-@Rarity.RarityName.ToLower()" colspan="4">@Rarity.RarityName</th>
                    }
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td class="boxed-gray">Stats / Level</td>
                    @foreach(var Rarity in Model.AvailableRarities) {
                        for(var i = 7; i <= 16; i += 3) {
                            <td class="boxed-@Rarity.RarityName.ToLower()">@i.ToString()</td>
                        }
                    }
                </tr>
                @foreach(var Stat in Model.AvailableStats) {
                    <tr>
                        <td class="boxed-gray">@Stat.StatName @Stat.StatModifier</td>
                        @foreach(var Rarity in Model.AvailableRarities) {
                            for(var i = 7; i <= 16; i += 3) {
                                <td class="boxed-@Rarity.RarityName.ToLower()">@Model.CalculateStat(Stat.StatName, Rarity.RarityName, Stat.StatModifier, i)</td>
                            }
                        }
                    </tr>
                }
            </tbody>
            <tfoot>
                <tr>
                    <th class="boxed-gray">Rarity</th>
                    @foreach(var Rarity in Model.AvailableRarities) {
                        <th class="boxed-@Rarity.RarityName.ToLower()" colspan="4">@Rarity.RarityName</th>
                    }
                </tr>
            </tfoot>
        </table>
    </div>
</div>
