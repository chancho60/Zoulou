@model Zoulou.ViewModels.MMEG.GlyphViewModel
@{
    ViewBag.Title = "MMEG - Glyphs";
}
<div class="row">
    <div class="col-md-12">
        <table class="cstm-table">
            <thead>
                <tr>
                    <th>Rarity</th>
                    @foreach(var Rarity in Model.AvailableRarities) {
                        <th colspan="4">@Rarity.ToString()</th>
                    }
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>Stats / Level</td>
                    @foreach(var Rarity in Model.AvailableRarities) {
                        for(var i = 7; i <= 16; i += 3) {
                            <td>@i.ToString()</td>
                        }
                    }
                </tr>
                @foreach(var Stat in Model.GetAvailableStats()) {
                    <tr>
                        <td>@Stat.Name @Stat.Modifier</td>
                        @foreach(var Rarity in Model.AvailableRarities) {
                            for(var i = 7; i <= 16; i += 3) {
                                <td>@Model.CalculateStat(Stat.Name, Rarity.ToString(), Stat.Modifier, i)</td>
                            }
                        }
                    </tr>
                }
            </tbody>
            <tfoot>
                <tr>
                    <th>Rarity</th>
                    @foreach(var Rarity in Model.AvailableRarities) {
                        <th colspan="4">@Rarity.ToString()</th>
                    }
                </tr>
            </tfoot>
        </table>
    </div>
</div>
